try{
real i=0;
  { int j = 1;
      real value = int1d(Th)(Ux* Uy);
    } varf myProblem ([Ux, Uy], [Uhy, Uhy])
      = int2d(Th)(
        grad(U)' * grad(Uh));
  


      //Inline   comment
    for (int i = 0; i < 10; ++i){
      cout << i << endl;     //    Inline comment 2
    }

    /*
    Some multiline
      comment
    */

    if (i == 0) {
      cout << "i = 0" << endl;
    } else if (i != 1) {
      cout << "i != 1" << endl;
    } else {
      cout << "i unknown" << endl;
    }
      
    <%# EJS COMMENT-%>
  
    <%
    let coucou = 'coucou'
    %>
  }catch(...){
    cout << "error" << endl;
  }